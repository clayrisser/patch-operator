images:
  patchOperator:
    repository: gitlab./patch-operator
    tag: 0.1.1

config:
  clusterProvider: infrastructure
  imagePullPolicy: IfNotPresent
  updateStrategy: RollingUpdate
  patchOperator:
    baseUrl: ""
    hostname: ""
    resources:
      requests:
        cpu: 100m
        memory: 128Mi
      limits:
        cpu: 500m
        memory: 256Mi
